Activity - 4
We use unordered array priority queue implementation because it takes constant time complexity
to insert an element into the queue whereas in other type of implementations it takes lot more
time like N and LogN time complexity.